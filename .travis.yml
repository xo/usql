language: go
go:
  - 1.5
  - 1.8
  - tip
services:
  - mysql
  - postgresql
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y unixodbc unixodbc-dev
  - go get github.com/mattn/goveralls
script:
  - go test -v ./stmt/...
